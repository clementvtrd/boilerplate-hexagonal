/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: {
        buildActivity: true,
    },
    buildActivityPosition: 'bottom-right',
    typedRoutes: true,

}

module.exports = nextConfig
