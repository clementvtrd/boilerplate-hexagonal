overwrite: true
schema: "http://nginx/graphql/"
documents: "src/**/*.gql"
generates:
  src/hooks/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
