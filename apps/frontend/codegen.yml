overwrite: true
schema: "http://nginx/graphql/"
documents: "src/**/*.gql"
generates:
  src/hooks/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
